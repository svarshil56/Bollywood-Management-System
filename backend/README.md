# Backend

This folder contains the Express API and PostgreSQL access layer.

## Structure

- `server.js` starts the Express server.
- `db.js` configures the PostgreSQL pool.
- `controllers/` contains request handlers.
- `models/` contains database queries.
- `routes/` defines API routes.
